#include <bits/stdc++.h>
using namespace std;

int main()
{
    freopen("input.txt","r",stdin);
    freopen("output.txt","w",stdout);

    int test=1, cased;
    scanf("%d", &cased);
    while(test<=cased){
   int num,i,counted=0;
   scanf("%d", &num);
   printf("Case %d: %d\n",test,num-1 );
    test++;
    }
    return 0;
}

